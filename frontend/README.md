# SaloneBaskit Frontend

This directory contains the React-based frontend for the SaloneBaskit e-commerce application, built with Vite.

## Development

To run the frontend development server, navigate to the root of the monorepo and run the `dev` script:

```sh
npm run dev
```
